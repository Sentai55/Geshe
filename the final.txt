local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer

--------------------------------------------------------------------------------
-- PENGATURAN KOORDINAT CHECKPOINT & PORTAL
--------------------------------------------------------------------------------
local CHECKPOINTS = {
    Vector3.new(-6141, -202, 587),  -- Room 1
    Vector3.new(-4090, 441, 1572),  -- Room 2
    Vector3.new(-4292, 446, 3175),  -- Room 3
    Vector3.new(-8871, 199, -1651), -- Room 4
    Vector3.new(-4373, 521, -1947), -- Room 5 (Terakhir)
}

local PORTAL_COORDS = {
    [2] = Vector3.new(-6420, -170, 585),   -- Portal setelah Room 1
    [3] = Vector3.new(-4336, 465, 1584),   -- Portal setelah Room 2
    [4] = Vector3.new(-4044, 468, 3152),   -- Portal setelah Room 3
    [5] = Vector3.new(-8644, 221, -1196),  -- Portal setelah Room 4
}

local SAFE_OFFSET_Y = 0

--------------------------------------------------------------------------------
-- VARIABEL STATUS
--------------------------------------------------------------------------------
local currentStageIndex = 1
local isFinished = false
local isTransitioning = false

--------------------------------------------------------------------------------
-- SISTEM NOCLIP OTOMATIS
--------------------------------------------------------------------------------
RunService.Stepped:Connect(function()
    local character = LocalPlayer.Character
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA("BasePart") and part.CanCollide then
                part.CanCollide = false
            end
        end
    end
end)

--------------------------------------------------------------------------------
-- FUNGSI CEK HASIL GAME
--------------------------------------------------------------------------------
local function checkGameFinished()
    local playerGui = LocalPlayer:FindFirstChild("PlayerGui")
    if playerGui then
        local resultGui = playerGui:FindFirstChild("ResultGui")
        if resultGui then
            local screenSettlement = resultGui:FindFirstChild("ScreenSettlement")
            if screenSettlement and screenSettlement.Visible == true then
                return true
            end
        end
    end
    return false
end

--------------------------------------------------------------------------------
-- FUNGSI HITUNG TOTAL NPC HIDUP
--------------------------------------------------------------------------------
local function getAliveNPCCount(folder)
    local count = 0
    if not folder then return 0 end
    for _, npc in ipairs(folder:GetChildren()) do
        local npcHumanoid = npc:FindFirstChildOfClass("Humanoid")
        if npcHumanoid and npcHumanoid.Health > 0 and npcHumanoid:GetState() ~= Enum.HumanoidStateType.Dead then
            count = count + 1
        end
    end
    return count
end

--------------------------------------------------------------------------------
-- LOGIKA UTAMA AUTO FARM STORY
--------------------------------------------------------------------------------
task.spawn(function()
    -- 1. DELAY AMAN 10 DETIK DI AWAL BUAT AUTO EXECUTE
    print("⏳ Menunggu delay awal 10 detik agar game ter-load sempurna...")
    task.wait(10)

    if not game:IsLoaded() then game.Loaded:Wait() end
    
    local EnemyFolder = workspace:FindFirstChild("EnemyNpc") or workspace:WaitForChild("EnemyNpc", 20)
    if not EnemyFolder then
        warn("Folder 'EnemyNpc' belum ter-load!")
        return
    end

    print("🚀 Auto Farm Story Dimulai!")

    -- TELEPORT AWAL KE ROOM 1
    local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    local hrp = character:WaitForChild("HumanoidRootPart", 15)
    if hrp then
        hrp.CFrame = CFrame.new(CHECKPOINTS[1])
    end
    
    -- PROTEKSI UTAMA: TUNGGU SAMPAI MUSUH ROOM 1 BENERAN MUNCUL!
    print("⏳ Menunggu NPC Room 1 Spawn...")
    local spawnTimeout = 0
    while getAliveNPCCount(EnemyFolder) == 0 and spawnTimeout < 15 do
        task.wait(0.5)
        spawnTimeout = spawnTimeout + 0.5
    end
    print("✅ NPC Ditemukan! Mulai Bantai...")

    while task.wait(0.05) do
        pcall(function()
            if isFinished or isTransitioning then return end

            local char = LocalPlayer.Character
            if not char or not char:FindFirstChild("HumanoidRootPart") then return end
            
            local charHrp = char.HumanoidRootPart
            local humanoid = char:FindFirstChildOfClass("Humanoid")

            -- A. CEK HASIL GAME (TAMAT)
            if checkGameFinished() then
                isFinished = true
                print("🏆 ScreenSettlement Visible! Game Selesai...")

                if humanoid then humanoid.AutoRotate = true end

                task.wait(3)

                local gameRoundRemote = ReplicatedStorage:WaitForChild("Remotes", 10):WaitForChild("GameRoundRE", 10)
                if gameRoundRemote then
                    gameRoundRemote:FireServer("VotePlayAgain")
                    print("✉️ VotePlayAgain Ditembak!")
                end
                return
            end

            -- B. PENAHAN GRAVITASI PERMANEN
            local freezeVel = charHrp:FindFirstChild("PositionLock")
            if not freezeVel then
                freezeVel = Instance.new("BodyVelocity")
                freezeVel.Name = "PositionLock"
                freezeVel.MaxForce = Vector3.new(0, math.huge, 0)
                freezeVel.Velocity = Vector3.zero
                freezeVel.Parent = charHrp
            end

            -- C. BANTAI NPC DI KOORDINAT AKTIF
            local currentPoint = CHECKPOINTS[currentStageIndex]
            local aliveCount = getAliveNPCCount(EnemyFolder)

            if aliveCount > 0 then
                if humanoid then humanoid.AutoRotate = false end

                -- Lock Posisi
                charHrp.CFrame = CFrame.new(currentPoint) * CFrame.new(0, SAFE_OFFSET_Y, 0)
                charHrp.AssemblyLinearVelocity = Vector3.zero
                charHrp.AssemblyAngularVelocity = Vector3.zero

            else
                -- PROSES PINDAH ROOM (HANYA JIKA MUSUH BENERAN HABIS)
                if currentStageIndex < #CHECKPOINTS then
                    isTransitioning = true
                    print("⚔️ NPC Room " .. currentStageIndex .. " Habis! Tahan 4 detik...")
                    
                    task.wait(4)

                    currentStageIndex = currentStageIndex + 1

                    -- Lepas Lock Gravitasi Sementara
                    local currentLock = charHrp:FindFirstChild("PositionLock")
                    if currentLock then currentLock:Destroy() end

                    -- Teleport Ke Portal
                    local portalPos = PORTAL_COORDS[currentStageIndex]
                    if portalPos then
                        print("🌀 Teleport ke Portal Room " .. currentStageIndex)
                        charHrp.CFrame = CFrame.new(portalPos)
                        task.wait(3)
                    end

                    -- Teleport Ke Room Bantai Berikutnya
                    local nextPoint = CHECKPOINTS[currentStageIndex]
                    charHrp.CFrame = CFrame.new(nextPoint)
                    print("📌 Teleport ke Room " .. currentStageIndex)

                    -- TUNGGU MUSUH DI ROOM BARU BENERAN SPAWN
                    print("⏳ Menunggu NPC Room " .. currentStageIndex .. " Spawn...")
                    local roomWait = 0
                    while getAliveNPCCount(EnemyFolder) == 0 and roomWait < 10 do
                        task.wait(0.5)
                        roomWait = roomWait + 0.5
                    end

                    isTransitioning = false
                else
                    -- Tetap Diam di Room 5
                    charHrp.CFrame = CFrame.new(currentPoint)
                end
            end
        end)
    end
end)